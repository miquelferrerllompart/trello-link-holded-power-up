"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
run_mode = "nonconcurrent"
setup = "ln -sf \"$CONDUCTOR_ROOT_PATH/.env\" .env && pnpm install --frozen-lockfile"

[scripts.run.run]
available_in = "local"
command = "pnpm run dev"
default = true
icon = "play"
